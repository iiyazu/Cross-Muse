NEXT_PUBLIC_XMUSE_CHAT_API_BASE_URL=http://localhost:8201/api/chat

# 下面两项只供 Next 服务端固定写代理使用，绝不能加 NEXT_PUBLIC_ 前缀。
# XMUSE_CHAT_API_BASE_URL=http://127.0.0.1:8201/api/chat
# XMUSE_OPERATOR_TOKEN=与 Chat API 进程相同的本地 token

# 当前边界仍是 localhost 单用户；固定代理不是远程认证方案。
