# Self-Iteration Runtime Closure Replay

Updated: 2026-06-10

This artifact records the minimal auditable xmuse self-iteration loop produced
from `docs/xmuse/archive/2026-06-proof-closure-legacy/self-iteration-runtime-closure-plan.md`.

It proves contract and fake/local runtime behavior only. It does not prove live
Ray, live Codex provider, live MemoryOS Lite, GitHub branch protection, or a
server-side merge run.

## Replay Path

```text
human request
-> GOD speech-act replay
-> frozen MissionBlueprintV1
-> BlueprintLaneDagRequest / BlueprintLaneDagPlan
-> LaneDispatchDecision
-> SubagentRuntimeContract
-> SelfIterationEvidenceBundle
-> ReviewOutcome / patch-forward lane
-> FakeGitHubOps PR record / merge readiness
-> REST-first MemoryOS writeback with FakeMemoryOSClient
```

## Human Request

```text
Execute the xmuse Self-Iteration Runtime Closure plan and produce one auditable
replay loop.
```

## GOD Speech-Act Transcript

Source fixture:

```text
src/xmuse_core/self_iteration/runtime_closure.py::build_self_iteration_replay_fixture
tests/xmuse/test_self_iteration_runtime_closure.py::test_replay_fixture_freezes_blueprint_and_blocks_unresolved_objection
```

Deterministic message refs:

| Ref | Sender | Act | Purpose |
| --- | --- | --- | --- |
| `message:gsa-001-propose` | `god-planner` | `propose` | Propose a bounded self-iteration closure loop. |
| `message:gsa-002-ask-memory` | `god-planner` | `ask` | Ask which MemoryOS namespace proves writeback. |
| `message:gsa-003-challenge-proof` | `god-review` | `challenge` | Require separation of contract/fake/live proof. |
| `message:gsa-004-object-live-claim` | `god-review` | `object` | Block unverified branch-protection/live-service claims. |
| `message:gsa-005-evidence-memory` | `god-memory` | `evidence` | Resolve the MemoryOS namespace question. |
| `message:gsa-006-evidence-proof-boundary` | `god-planner` | `evidence` | Resolve proof-boundary challenge and objection. |
| `message:gsa-007-vote-freeze` | `god-review` | `vote` | Approve freeze after evidence. |
| `message:gsa-008-decide-freeze` | `god-convenor` | `decide` | Freeze blueprint and dispatch execution sample. |
| `message:gsa-009-handoff-execute` | `god-convenor` | `handoff` | Hand bounded execution to `god-execute`. |

Freeze is denied if the proof-boundary evidence is removed; the test asserts the
guard fails on unresolved blockers or unanswered replies.

## Frozen Blueprint

Source:

```text
src/xmuse_core/self_iteration/runtime_closure.py::derive_frozen_self_iteration_blueprint
```

Blueprint identity:

```text
blueprint_id: bp-self-iteration-runtime-closure
conversation_id: conv-self-iteration-runtime-closure
revision: 1
status: frozen
ref: blueprint:bp-self-iteration-runtime-closure:1
approved_by: god-review, god-convenor
```

Acceptance contracts:

- Replay derives a frozen `MissionBlueprintV1` from structured speech acts.
- Blueprint converts to typed laneDAG and scheduler dispatch decisions.
- `SubagentRuntimeContract` serializes execution authority and scope.
- Evidence bundle drives review approval and patch-forward rejection paths.
- MemoryOS fake writeback preserves actor, namespace, source refs, and layer.
- Documentation states proof level for every claim.

## Feature/Lane/LaneDAG

Source:

```text
src/xmuse_core/self_iteration/runtime_closure.py::build_self_iteration_lane_dag_request
tests/xmuse/test_self_iteration_runtime_closure.py::test_blueprint_builds_authoritative_typed_lanedag_and_dispatch
```

Feature set:

- `feature-github-truth`
- `feature-replay-lanedag`
- `feature-runtime-evidence`
- `feature-review-writeback-docs`

Lane set:

- `lane-github-truth`
- `lane-replay-lanedag`
- `lane-runtime-evidence`
- `lane-review-writeback-docs`

Typed edges:

- `hard_dep`: feature dependency edges generated by `BlueprintLaneDagService`.
- `soft_dep`: `lane-github-truth -> lane-replay-lanedag`.
- `review_dep`: `lane-runtime-evidence -> lane-review-writeback-docs`.
- `artifact_dep`: `lane-replay-lanedag -> lane-review-writeback-docs`.

Dispatch evidence:

```text
lane-runtime-evidence becomes ready only after lane-replay-lanedag is approved.
```

`feature_lanes.json` is not read or treated as authority.

## Runtime Contract

Source:

```text
src/xmuse_core/platform/execution/subagent_runtime.py::SubagentRuntimeContract
src/xmuse_core/self_iteration/runtime_closure.py::build_self_iteration_runtime_contract
tests/xmuse/test_self_iteration_runtime_closure.py::test_runtime_contract_serializes_required_self_iteration_fields
```

Required self-iteration fields are populated:

- `blueprint_id`
- `feature_id`
- `lane_id`
- `depends_on`
- `worktree_path`
- `allowed_files`
- `allowed_tools`
- `acceptance_criteria`
- `required_checks`
- `source_context_refs`
- `memory_context_ref`
- `rollback_plan`
- `review_profile`

The contract validates changed paths against `write_scope` before producing
evidence.

## Evidence And Review

Source:

```text
src/xmuse_core/self_iteration/runtime_closure.py::build_self_iteration_evidence_bundle
src/xmuse_core/self_iteration/runtime_closure.py::review_self_iteration_evidence
tests/xmuse/test_self_iteration_runtime_closure.py::test_review_failure_appends_patch_forward_without_overwriting_failed_lane
```

Evidence bundle:

```text
schema_version: self_iteration_evidence_bundle.v1
evidence_id: self-iteration-lane-runtime-evidence
proof_level: fake_runtime_proof
```

The bundle contains changed files, commands run, test results, source refs,
memory refs, risk notes, and rollback notes.

Review behavior:

- Approval creates an `approved` `ReviewOutcome` with evidence refs.
- Rejection creates a `changes_requested` `ReviewOutcome`.
- Rejection appends `lane-runtime-evidence-patch-1` instead of overwriting
  `lane-runtime-evidence`.

## GitHub Gate Evidence

Source:

```text
.github/workflows/xmuse-ci.yml
CODEOWNERS
.github/pull_request_template.md
src/xmuse_core/self_iteration/runtime_closure.py::read_github_truth_evidence
```

Required check names aligned with workflow jobs:

- `quality-gates`
- `contract-smoke-gates`
- `real-runtime-integration-gate`

Local repository evidence proves:

- workflow job names exist and are unique in `.github/workflows/xmuse-ci.yml`;
- CODEOWNERS covers chat, structuring, platform, integrations, providers,
  `.github`, and `docs/xmuse`;
- PR template includes blueprint, feature, lane, dependency, memory, gate,
  review, rollback, provider, and privacy fields;
- fake PR rendering carries blueprint/lane/gate/evidence/rollback metadata;
- merge readiness requires successful checks and review evidence.

Not proven by local evidence:

- latest GitHub Actions run status for the pushed commit;
- server-side branch protection;
- server-side required reviewer enforcement.

Those remain server/admin evidence, not fake runtime proof.

## MemoryOS Writeback

Source:

```text
src/xmuse_core/self_iteration/runtime_closure.py::write_self_iteration_memory_evidence
tests/xmuse/test_self_iteration_runtime_closure.py::test_memoryos_writeback_preserves_namespace_actor_layer_and_sources
```

Default CI uses `FakeMemoryOSClient` through the REST-first
`MemoryOSClientProtocol`. It writes four events:

- `blueprint_frozen`
- `feature_reworked`
- `review_verdict_finalized`
- `merge_readiness_evaluated`

`merge_readiness_evaluated` is not a real GitHub merge fact. `pr_merged` is
reserved for server-side GitHub merge evidence.

The task namespace preserves repo, workspace, god, conversation, thread,
blueprint, feature, and lane dimensions:

```text
memory://repo/iiyazu/Cross-Muse/workspace/xmuse/conversation/conv-self-iteration-runtime-closure/thread/thread-self-iteration-runtime-closure/god/god-execute/blueprint/bp-self-iteration-runtime-closure/feature/feature-runtime-evidence/lane/lane-runtime-evidence
```

The fake writeback test asserts actor identity, `task_state` memory layer,
commit-aligned source refs, and writeback metadata. Live MemoryOS Lite proof
remains opt-in and is tracked as debt.

## Long-Run Replay Summary

Source:

```text
src/xmuse_core/self_iteration/runtime_closure.py::build_self_iteration_long_run_replay_summary
tests/xmuse/test_self_iteration_runtime_closure.py::test_long_run_replay_summary_records_heartbeat_review_and_patch_lineage
tests/xmuse/test_self_iteration_runtime_closure.py::test_long_run_replay_summary_records_slo_violation_from_simulated_time
tests/xmuse/test_self_iteration_runtime_closure.py::test_long_run_replay_summary_records_review_snapshot_slo_violation
```

The replay summary is a contract/fake-runtime evidence artifact for long-running
closure runs. It records ordered logical heartbeats for:

- lane evidence;
- review verdict;
- patch-forward lineage;
- merge readiness.

The default summary preserves proof levels from the underlying artifacts. It
includes `fake_runtime_proof` and `contract_proof`, and explicitly excludes
live service or server-side enforcement proof unless those external evidences
are captured separately.

`merge_readiness_kind` remains `merge_readiness_evaluated` and
`real_merge_event` remains `false` in the default path.

The summary also includes deterministic SLO audit fields:

- `max_heartbeat_gap_minutes`;
- `max_review_snapshot_gap_minutes`;
- `slo_status`;
- `slo_violations`.

These fields let tests simulate long-run timing without sleeping. A heartbeat
gap over 15 minutes or a review snapshot gap over 45 minutes marks the replay
summary as `violated`. This remains replay/contract evidence only; it does not
prove a live long-running provider session emitted real heartbeats.

The builder rejects live/real proof levels in this default replay summary and
requires simulated heartbeat timestamps to be monotonic by `heartbeat_seq`.

## Proof Levels

| Surface | Current evidence | Proof level |
| --- | --- | --- |
| Speech-act replay and freeze guard | deterministic fixture and focused test | contract proof |
| Blueprint to laneDAG | `BlueprintLaneDagService` plan and dispatch test | contract proof |
| Subagent runtime contract | serialization and write-scope tests | contract proof |
| Evidence bundle | fake/local evidence model and review tests | fake runtime proof |
| Review patch-forward | patch lane lineage test | contract proof |
| GitHub PR metadata | `FakeGitHubOps` body and merge readiness | contract proof |
| GitHub merge fact | not produced by fake/local closure | manual gap |
| Long-run replay summary | logical heartbeat/review/patch-forward sequence | contract/fake runtime proof |
| Workflow/CODEOWNERS/template | local file evidence | contract proof |
| Branch protection | not verified | manual gap |
| MemoryOS writeback | `FakeMemoryOSClient` through REST-first protocol | fake runtime proof |
| Live Ray/Codex/MemoryOS | not run in default CI | manual gap |

## Validation Commands

Focused validation for this artifact:

```bash
uv run ruff check src/xmuse_core/self_iteration/runtime_closure.py src/xmuse_core/self_iteration/__init__.py src/xmuse_core/platform/execution/subagent_runtime.py tests/xmuse/test_self_iteration_runtime_closure.py
uv run pytest -q tests/xmuse/test_self_iteration_runtime_closure.py
uv run mypy src/xmuse_core/self_iteration/runtime_closure.py src/xmuse_core/platform/execution/subagent_runtime.py
```

Full goal validation also runs the repository lint gate and existing #13-#27
contract smoke set before closing the tracking issues.
